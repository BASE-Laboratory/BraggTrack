# Data directory

Versioned **input** data for development and CI lives under [`sample_operando/`](sample_operando/README.md).

Generated outputs belong in **`artifacts/`** at the repository root (gitignored).
